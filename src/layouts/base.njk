<!DOCTYPE html>
<html lang="{{ meta.lang }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ meta.title }}</title>

        <link rel="stylesheet" href="{{ '/assets/styles/main.css' | url }}">
        {# Google font #}
        <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;1,300;1,400&display=swap" rel="stylesheet">
        {# animation library from https://animate.style/ #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>

        {% include "meta.njk" %}
    </head>
    <body>
        <div hidden="hidden">{% iconsprite %}</div>
        <a href="#main" class="sr-skip-link">skip to main content</a>

        <div class="layout" role="document">
            {% include "header.njk" %}
            <main id="main" class="main" tabindex="-1">
                <div class="container">
                    {{ content | safe }}
                </div>
            </main>
            {% include "footer.njk" %}
        </div>

        <script type="text/javascript" src="{{ '/assets/scripts/main.js' | url }}"></script>
    </body>
</html>